#include "main.h"

/**
 * string_toupper - converts a string to uppercase
 * @str: string to reverse
 * Return: str
 */

char *string_toupper(char *str)
{
	while (str)
	{
		if (*str >= 97 && *str <= 122)
			*str -= 32;
		str++;
	}
	return (str);
}
