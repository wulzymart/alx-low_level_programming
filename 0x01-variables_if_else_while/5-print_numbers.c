#include <stdio.h>

/**
 * main - single unit numbers
 * Description: print out single unit base 10 numbers
 * Return: Success
 */

int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%i\n", n);
		n++;
	}
	return (0);
}
